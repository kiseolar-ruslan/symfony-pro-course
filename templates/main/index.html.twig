{% extends 'base.html.twig' %}

{% block title %}Home page{{ parent() }}{% endblock %}

{% block content %}
    {% if user %}
        <div>
            <p>Go to <a href="{{ path('all_statistic') }}">statistics</a></p>
        </div>
    {% else %}
        <p>Hi User, to use our service you need to authorize :)</p>
    {% endif %}
{% endblock %}
