{% extends 'base.html.twig' %}
{% set title = 'User list' %}
{% block title %}{{ title }}{{ parent() }}{% endblock %}

{% block breadCrumbs %}
    {{ parent() }} -> <a href="{{ path(app.current_route) }}">{{ title }}</a>
{% endblock %}

{% block content %}
    <div class="example-wrapper">
        <h1>Users list ✅</h1>
        <ul style="list-style-type: none">
            {% for user in users %}
                <li style="margin-bottom: 10px">{{ include('user/user_info_template.html.twig') }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
